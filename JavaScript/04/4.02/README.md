Аналогично предыдущему, однако теперь у блоков ```input``` есть ```value``` и ```id```. Последнее используется для обращения к отдельному блоку, ```value``` - чась вывода. Вызов функции вывода так же при нажатии на блок.