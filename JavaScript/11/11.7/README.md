Аналогично предыдущему. Важно использовать не ```textContent```, а ```innerText``` для блока вывода, так как иначе переносы ```\n``` не учитываются. Также надо отметить, что как и в ```11.1``` послед нажатия кнопки есть задержка.